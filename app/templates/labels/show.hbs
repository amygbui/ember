<h2>{{model.name}}</h2>
<button {{action 'deleteLabel' model}}>Delete</button>

<br />{{#link-to 'labels.edit' model.id}}
Edit
{{/link-to}}

<br /><br />
<ul>
  {{#each model.artists as |artist|}}
    <li>{{#link-to 'artist.show' artist}}
          {{artist.name}}
        {{/link-to}}
    </li>
  {{/each}}
</ul>
{{outlet}}
