<h2>{{model.name}}</h2>

Artists
<ul>
  {{#each model.artists as |artist|}}
    <li>{{#link-to 'artists.show' artist.id}}
          {{artist.name}}
        {{/link-to}}
    </li>
  {{/each}}
</ul>

<br />
<button>{{#link-to 'labels.edit' model.id}}Edit{{/link-to}}</button>
<button {{action 'deleteLabel' model}}>Delete Label</button>
<br /><br />
